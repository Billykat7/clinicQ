"""Queues: a clinic's queues, calling the next patient, and the tickets in each queue.

Issue 18 declares the module's authorization footprint (:mod:`.rbac_manifest`) ahead of the module
itself. Queues and rooms land with Issue 25, tickets with Issue 39, call-next with Issue 42.
"""
