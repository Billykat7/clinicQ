"""Widgets — the worked example module.

A vertical slice of everything a real module needs and nothing it does not: a model, schemas, a
service, a router, and an RBAC manifest that declares its own resources and default grants. Read it
in that order; every file says what its counterpart in your own module has to do.

Delete it once you have your own: ``make delete-example-module``.
"""
