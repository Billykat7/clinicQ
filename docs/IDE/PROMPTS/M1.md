Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #1 as described in docs/GITHUB/ISSUES/M1/ISSUE_1_auth_model_security_core.md.
Create a git branch Issue/1/auth-model-security-core (brief concise name).

Then create a PR_1_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #1.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1 (https://github.com/Billykat7/clinicq/milestone/1 ).
If possible, it should link the PR to Milestones #1. Create the PR with PR_*_DESCRIPTION.md for this branch and push it
linked its issue's milestones (On Github). This work MUST be done following the best coding practices, highest industry standards,
the latest language (Python 3.14) docs and this project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 1: *"

Create a corresponding docs/GITHUB/RELEASES docs too -- Assumming the next tag based on the committed tags. commit

------------------------------------------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #2 as described in docs/GITHUB/ISSUES/M1/ISSUE_2_signup_email_activation.md.
Create a git branch Issue/2/signup-email-activation (brief concise name).

Then create a PR_2_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #2.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1 (https://github.com/Billykat7/clinicq/milestone/1 ).
If possible, it should link the PR to Milestones #1. Create the PR with PR_*_DESCRIPTION.md for this branch and push it
linked its issue's milestones (On Github). This work MUST be done following the best coding practices, highest industry standards,
the latest language (Python 3.14) docs and this project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 2: *"

Create a corresponding docs/GITHUB/RELEASES docs too -- Assumming the next tag based on the committed tags. commit

------------------------------------------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #3 as described in docs/GITHUB/ISSUES/M1/ISSUE_3_signin_refresh_sessions.md.
Create a git branch Issue/3/signin-refresh-sessions (brief concise name).

Then create a PR_3_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #3.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1 (https://github.com/Billykat7/clinicq/milestone/1 ).
If possible, it should link the PR to Milestones #1. Create the PR with PR_*_DESCRIPTION.md for this branch and push it
linked its issue's milestones (On Github). This work MUST be done following the best coding practices, highest industry standards,
the latest language (Python 3.14) docs and this project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 3: *"

Create a corresponding docs/GITHUB/RELEASES docs too -- Assumming the next tag based on the committed tags. commit

------------------------------------------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #4 as described in docs/GITHUB/ISSUES/M1/ISSUE_4_users_email_crud.md.
Create a git branch Issue/4/users-email-crud (brief concise name).

Then create a PR_4_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #4.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1 (https://github.com/Billykat7/clinicq/milestone/1 ).
If possible, it should link the PR to Milestones #1. Create the PR with PR_*_DESCRIPTION.md for this branch and push it
linked its issue's milestones (On Github). This work MUST be done following the best coding practices, highest industry standards,
the latest language (Python 3.14) docs and this project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 4: *"

Create a corresponding docs/GITHUB/RELEASES docs too -- Assumming the next tag based on the committed tags. commit

------------------------------------------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #5 as described in docs/GITHUB/ISSUES/M1/ISSUE_5_rbac_model_enforcement.md.
Create a git branch Issue/5/rbac-model-enforcement (brief concise name).

Then create a PR_5_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #5.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1 (https://github.com/Billykat7/clinicq/milestone/1 ).
If possible, it should link the PR to Milestones #1. Create the PR with PR_*_DESCRIPTION.md for this branch and push it
linked its issue's milestones (On Github). This work MUST be done following the best coding practices, highest industry standards,
the latest language (Python 3.14) docs and this project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 5: *"

Create a corresponding docs/GITHUB/RELEASES docs too -- Assumming the next tag based on the committed tags. commit

------------------------------------------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #6 as described in docs/GITHUB/ISSUES/M1/ISSUE_6_rbac_admin_api.md.
Create a git branch Issue/6/rbac-admin-api (brief concise name).

Then create a PR_6_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #6.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1 (https://github.com/Billykat7/clinicq/milestone/1 ).
If possible, it should link the PR to Milestones #1. Create the PR with PR_*_DESCRIPTION.md for this branch and push it
linked its issue's milestones (On Github). This work MUST be done following the best coding practices, highest industry standards,
the latest language (Python 3.14) docs and this project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 6: *"

Create a corresponding docs/GITHUB/RELEASES docs too -- Assumming the next tag based on the committed tags. commit

------------------------------------------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #7 as described in docs/GITHUB/ISSUES/M1/ISSUE_7_s3_logging_pipeline.md.
Create a git branch Issue/7/s3-logging-pipeline (brief concise name).

Then create a PR_7_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #7.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1 (https://github.com/Billykat7/clinicq/milestone/1 ).
If possible, it should link the PR to Milestones #1. Create the PR with PR_*_DESCRIPTION.md for this branch and push it
linked its issue's milestones (On Github). This work MUST be done following the best coding practices, highest industry standards,
the latest language (Python 3.14) docs and this project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 7: *"

Create a corresponding docs/GITHUB/RELEASES docs too -- Assumming the next tag based on the committed tags. commit

------------------------------------------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #8 as described in docs/GITHUB/ISSUES/M1/ISSUE_8_admin_logs_viewer_api.md.
Create a git branch Issue/8/admin-logs-viewer-api (brief concise name).

Then create a PR_8_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #8.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1 (https://github.com/Billykat7/clinicq/milestone/1 ).
If possible, it should link the PR to Milestones #1. Create the PR with PR_*_DESCRIPTION.md for this branch and push it
linked its issue's milestones (On Github). This work MUST be done following the best coding practices, highest industry standards,
the latest language (Python 3.14) docs and this project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 8: *"

Create a corresponding docs/GITHUB/RELEASES docs too -- Assumming the next tag based on the committed tags. commit

------------------------------------------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #9 as described in docs/GITHUB/ISSUES/M1/ISSUE_9_admin_dashboard_ui.md.
Create a git branch Issue/9/admin-dashboard-ui (brief concise name).

Then create a PR_9_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #9.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1 (https://github.com/Billykat7/clinicq/milestone/1 ).
If possible, it should link the PR to Milestones #1. Create the PR with PR_*_DESCRIPTION.md for this branch and push it
linked its issue's milestones (On Github). This work MUST be done following the best coding practices, highest industry standards,
the latest language (Python 3.14) docs and this project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 9: *"

Create a corresponding docs/GITHUB/RELEASES docs too -- Assumming the next tag based on the committed tags. commit

------------------------------------------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #10 as described in docs/GITHUB/ISSUES/M1/ISSUE_10_ci_local_harness.md.
Create a git branch Issue/10/ci-local-harness (brief concise name).

Then create a PR_10_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #10.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1 (https://github.com/Billykat7/clinicq/milestone/1 ).
If possible, it should link the PR to Milestones #1. Create the PR with PR_*_DESCRIPTION.md for this branch and push it
linked its issue's milestones (On Github). This work MUST be done following the best coding practices, highest industry standards,
the latest language (Python 3.14) docs and this project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 10: *"

Create a corresponding docs/GITHUB/RELEASES docs too -- Assumming the next tag based on the committed tags. commit

------------------------------------------------------------------------------------------------------------------------
