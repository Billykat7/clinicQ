The eight prompts below are Milestone 1 (#1–#8), the foundation: a running, tested, empty application six people can work in without
stepping on each other. Specs: docs/GITHUB/ISSUES/M1/. Milestone doc: docs/GITHUB/MILESTONES/M1_foundation_local_ci.md. This is the first
milestone; nothing precedes it. Most of it is already scaffolded (Issue #1 is in progress on its branch), so every prompt below starts by
checking the acceptance criteria against what exists. Created on GitHub 2026-09-10: milestone 1
(https://github.com/Billykat7/clinicQ/milestone/1) and issues #1–#8. Local spec numbers and GitHub issue numbers are the same in this
repository, so #N below is Issue N everywhere. Finish #1 first; then #2, #4, #5 and #6 in parallel; then #3 (it needs the database from #2);
#7 and #8 last.

----------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #1 as described in
docs/GITHUB/ISSUES/M1/ISSUE_1_repo_scaffold_app_factory.md.
This issue is already in progress on its branch (Issue/1/repo-scaffold-initial-setup): the scaffold, the landing page and the rebrand are
committed, so this is review-and-fill-the-gap, not a fresh start. Read the spec's Starting point first. The spec was written for uv and an
app/ package; the repository uses requirements.txt with setuptools and a src/ package. Do NOT switch either as part of this issue: record
the choice as open decision 3 in docs/GITHUB/ISSUES/README.md and make the acceptance criteria true for the layout that exists. Verify each
criterion against the tree rather than ticking it from memory: create_app() used by both the server and the test client, a clear startup
error for a missing required setting, ruff and mypy clean. README.md's Getting started still shows the old uv and app.main commands; rewrite
it with the commands that actually work today, and run every one of them on a clean checkout before you write it down.
Continue on the existing git branch Issue/1/repo-scaffold-initial-setup (it already carries this work).

Then create a PR_1_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #1.
Follow docs/GITHUB/PR/PR_TEMPLATE.md. This PR_1_DESCRIPTION.md becomes the pattern every later PR description follows.
This PR belongs to an issue linked to Milestones #1. If possible, it should link the PR to Milestones #1. Create the PR
with PR_*_DESCRIPTION.md for this branch and push it linked its issue's milestones (On Github). This work MUST be done
following the best coding practices, highest industry standards, the latest language (Python 3.14) docs and this
project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 1: *", 
but must not contain "Co-authored-by: Cursor...*" or "Co-Authored-By: Claude...*"

----------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #2 as described in
docs/GITHUB/ISSUES/M1/ISSUE_2_docker_dev_stack_postgis_redis.md.
This issue depends on #1. Read the spec's Starting point first: infra/docker/docker-compose.db.yml (make db-up) and
infra/docker/docker-compose.yml (make docker-up) already exist, but the image is postgis/postgis:16-3.4 while the spec says PostgreSQL 18,
and Redis exists only in the production compose file. Decide 16 versus 18 explicitly (open decision 4 in docs/GITHUB/ISSUES/README.md) and
make CI, the compose files and the spec agree; do not leave two versions in play. Add Redis with a health check to the development stack.
Keep every Docker file under infra/docker/ (.cursor/rules/infra-layout.mdc). There is no tracked .env.example yet: add only DATABASE_URL and
REDIS_URL here, because #12 owns the full file. Demonstrate, do not assert: SELECT postgis_version() against the compose database, data
surviving a restart, and the documented wipe-and-recreate one-liner, each shown in the PR.
Create a git branch Issue/2/docker-dev-stack (brief concise name).

Then create a PR_2_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #2.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1. If possible, it should link the PR to Milestones #1. Create the PR
with PR_*_DESCRIPTION.md for this branch and push it linked its issue's milestones (On Github). This work MUST be done
following the best coding practices, highest industry standards, the latest language (Python 3.14) docs and this
project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 2: *", 
but must not contain "Co-authored-by: Cursor...*" or "Co-Authored-By: Claude...*"

----------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #4 as described in
docs/GITHUB/ISSUES/M1/ISSUE_4_shared_kernel_enums_time_errors.md.
This issue depends on #1 and can run in parallel with #2, #5 and #6. Put the ClinicQ enums (SiteSector, TicketStatus, TicketSource,
DisplayMode) in src/commons/enums.py beside the kernel's, and extend the existing UserRole rather than adding a second StaffRole enum. The
error envelope already lives in src/commons/exceptions.py: extend it, do not fork it. The kernel has no now_sast() helper yet. Add the time
helpers in src/commons/ on top of APP_TIMEZONE, following .cursor/rules/timezone-johannesburg.mdc (business datetimes in
Africa/Johannesburg, UTC only where a standard requires it). The conventions guard test is the point of this issue, because it is what makes
non-negotiable 5 in docs/guideline.md enforceable: prove it fails on a naive datetime.now() and on a magic status string by showing both
failures in the PR.
Create a git branch Issue/4/shared-kernel (brief concise name).

Then create a PR_4_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #4.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1. If possible, it should link the PR to Milestones #1. Create the PR
with PR_*_DESCRIPTION.md for this branch and push it linked its issue's milestones (On Github). This work MUST be done
following the best coding practices, highest industry standards, the latest language (Python 3.14) docs and this
project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 4: *", 
but must not contain "Co-authored-by: Cursor...*" or "Co-Authored-By: Claude...*"

----------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #5 as described in
docs/GITHUB/ISSUES/M1/ISSUE_5_base_ui_shell_tailwind_htmx.md.
This issue depends on #1 and can run in parallel with #2, #4 and #6. It carries a DECISION before any code: the spec asks for Tailwind, but
the kernel already styles every screen with hand-written design tokens (src/static/css/site.css, admin.css, landing.css) in light and dark
themes. Settle open decision 2 in docs/GITHUB/ISSUES/README.md with the team first, and write down why. Whichever wins, the CSP forbids
inline styles and scripts and tests/unit/platform/test_no_inline_styles.py enforces it, so every new component keeps its CSS in
src/static/css/ and its JS in src/static/js/. Build the three layouts (patient, dashboard, board) from ONE token set.
.cursor/rules/testing-strategy.mdc forbids tests that assert rendered HTML: cover the behaviour instead, and attach screenshots of all three
layouts in light and dark to the PR.
Create a git branch Issue/5/base-ui-shell (brief concise name).

Then create a PR_5_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #5.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1. If possible, it should link the PR to Milestones #1. Create the PR
with PR_*_DESCRIPTION.md for this branch and push it linked its issue's milestones (On Github). This work MUST be done
following the best coding practices, highest industry standards, the latest language (Python 3.14) docs and this
project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 5: *", 
but must not contain "Co-authored-by: Cursor...*" or "Co-Authored-By: Claude...*"

----------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #6 as described in
docs/GITHUB/ISSUES/M1/ISSUE_6_logging_request_context_health.md.
This issue depends on #1 and can run in parallel with #2, #4 and #5. Most of it exists: src/core/logging_config.py (JSON logging),
src/core/request_logging.py (request id, path, client IP) and src/core/health.py (/health, /health/live, /health/ready). Verify before
adding anything. What is missing: site_id and actor_id in the log context, a Redis check in readiness (it checks the database, migrations
and S3 today), redaction of phone numbers and OTPs, and the request id echoed in a response header. Redaction must happen in the logging
layer, not at each call site: a phone number logged from any module must come out masked, proven by a test that logs one. Liveness must stay
dependency-free, because a stopped database has to fail readiness without triggering a restart loop.
Create a git branch Issue/6/logging-request-context-health (brief concise name).

Then create a PR_6_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #6.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1. If possible, it should link the PR to Milestones #1. Create the PR
with PR_*_DESCRIPTION.md for this branch and push it linked its issue's milestones (On Github). This work MUST be done
following the best coding practices, highest industry standards, the latest language (Python 3.14) docs and this
project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 6: *", 
but must not contain "Co-authored-by: Cursor...*" or "Co-Authored-By: Claude...*"

----------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #3 as described in
docs/GITHUB/ISSUES/M1/ISSUE_3_sqlalchemy_alembic_baseline.md.
This issue depends on #2 (the database to run against). Most of it exists, so verify before changing anything. Sync and async sessions
(get_db, get_async_db) are in src/database/session.py, the constraint naming convention is in src/database/models/base.py, and the baseline
migration already runs CREATE EXTENSION IF NOT EXISTS postgis WITH SCHEMA public. The spec assumes async everywhere while most kernel routes
use the sync session: settle open decision 5 in docs/GITHUB/ISSUES/README.md and state the convention new modules follow. Then prove each
criterion with a test rather than by reading code: alembic upgrade head on an empty database, a downgrade-to-base-and-back round trip, an
empty autogenerate diff straight after an upgrade, and a session that rolls back on an exception without leaking a connection.
Create a git branch Issue/3/sqlalchemy-alembic-baseline (brief concise name).

Then create a PR_3_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #3.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1. If possible, it should link the PR to Milestones #1. Create the PR
with PR_*_DESCRIPTION.md for this branch and push it linked its issue's milestones (On Github). This work MUST be done
following the best coding practices, highest industry standards, the latest language (Python 3.14) docs and this
project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 3: *", 
but must not contain "Co-authored-by: Cursor...*" or "Co-Authored-By: Claude...*"

----------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #7 as described in
docs/GITHUB/ISSUES/M1/ISSUE_7_ci_local_harness_precommit.md.
This issue depends on #1 and #3; it can run in parallel with #8. scripts/ci-local.sh already runs ruff, mypy, pytest and a Docker build,
with pip-audit, Trivy and a secret scan (make check, make check-fast), so this issue is about the gaps. make hooks expects a
.pre-commit-config.yaml that does not exist, and tests/unit/platform/test_scanning_config.py expects a .gitleaks.toml that does not exist
either, which is why it fails today. Add both. Agree the coverage threshold with the team and enforce it in the script, and add the unit,
integration and slow pytest markers. Demonstrate, do not assert: a commit containing a fake secret must be shown blocked by pre-commit in
the PR. Document the pre-push workflow in CONTRIBUTING.md.
Create a git branch Issue/7/ci-local-harness-precommit (brief concise name).

Then create a PR_7_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #7.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1. If possible, it should link the PR to Milestones #1. Create the PR
with PR_*_DESCRIPTION.md for this branch and push it linked its issue's milestones (On Github). This work MUST be done
following the best coding practices, highest industry standards, the latest language (Python 3.14) docs and this
project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 7: *", 
but must not contain "Co-authored-by: Cursor...*" or "Co-Authored-By: Claude...*"

----------------------------------------------------------------------------------------

Do the work (if some have been completed, if so simply review & fill the gap or update) for Issue #8 as described in
docs/GITHUB/ISSUES/M1/ISSUE_8_test_factories_seed_data.md.
This issue depends on #3 and #4; it can run in parallel with #7. The sites, queues and tickets models do not exist until #23, #25 and #39,
so build the factory framework and the patient and staff factories now, and add the site, queue and ticket factories alongside those issues
or against agreed stubs; say which in the PR. Follow the shape of the kernel's seeding (scripts/db/seed-dev-user.py,
scripts/db/seed_rbac.py) for scripts/db/seed_dev_data.py. The seed script must be idempotent and must REFUSE to run against a production
DATABASE_URL: prove both, a second run creating no duplicates and a production URL rejected. Use real Gauteng and KZN coordinates, because
discovery (#31) and the map (#33) will be judged against them.
Create a git branch Issue/8/test-factories-seed-data (brief concise name).

Then create a PR_8_DESCRIPTION.md to be placed in docs/GITHUB/PR/M1 and it should close GitHub Issue #8.
Look at docs/GITHUB/PR/M1/PR_1_DESCRIPTION.md. This is the pattern to follow for the remaining PR descriptions.
This PR belongs to an issue linked to Milestones #1. If possible, it should link the PR to Milestones #1. Create the PR
with PR_*_DESCRIPTION.md for this branch and push it linked its issue's milestones (On Github). This work MUST be done
following the best coding practices, highest industry standards, the latest language (Python 3.14) docs and this
project's rules defined in .cursor/rules.

Commit. But the message starts with the branch name. i.e: "Issue 8: *", 
but must not contain "Co-authored-by: Cursor...*" or "Co-Authored-By: Claude...*"

Create the corresponding docs/GITHUB/RELEASES doc too: RELEASE_v0_1_0.md for tag v0.1.0, from the template in
docs/GITHUB/RELEASES/README.md. The tag follows the milestone's LAST issue, so write it only once every M1 issue (#1–#8) is merged, and
record the migrations and any known issues honestly. commit
